<%= form_with(model: mytask) do |form| %>
  <% if mytask.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mytask.errors.count, "error") %> prohibited this mytask from being saved:</h2>

      <ul>
        <% mytask.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

    <%= form.hidden_field :user_id %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

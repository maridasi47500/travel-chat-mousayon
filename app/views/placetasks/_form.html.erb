<%= form_with(model: placetask) do |form| %>
  <% if placetask.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(placetask.errors.count, "error") %> prohibited this placetask from being saved:</h2>

      <ul>
        <% placetask.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :place_id, style: "display: block" %>
    <%= form.select(:place_id, Place.all.order(:name).collect{|h|[h.name, h.id]}).html_safe %>
  </div>

    <%= form.hidden_field :user_id %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
